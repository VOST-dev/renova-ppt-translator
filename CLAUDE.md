# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What This Project Does

PowerPoint document translation service. Users upload `.pptx` files, specify languages, and the backend submits AWS Translate batch jobs. The frontend polls job status and provides download links when complete.

## Document

- 配下にプロジェクトに関するドキュメントは `docs/` 配下に保存
- `docs/tasks` と `docs/superpowers/` はタスク指示用のドキュメントになので、明示的な指示がなければ参照しない